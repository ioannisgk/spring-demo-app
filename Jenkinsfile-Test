pipeline {
    agent any
    tools {
        docker 'docker'
    }
    stages {
        stage('Docker Test') {
            steps {
                sh 'docker --version'
                sh 'docker run hello-world'
            }
        }
    }
}
